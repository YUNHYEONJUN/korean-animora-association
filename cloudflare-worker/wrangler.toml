name = "animora-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 환경 변수 (비밀 키는 `npx wrangler secret put OPENAI_API_KEY`로 설정)
[vars]
OPENAI_MODEL = "gpt-4o"
ALLOWED_ORIGINS = "https://yunhyeonjun.github.io"
